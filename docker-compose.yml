services:
  iopenwrt:
    image: amstel/iopenwrt:latest
    container_name: iopenwrt-controller
    ports:
      - "${PORT:-8780}:${PORT:-8780}"
    volumes:
      - /mnt/user/appdata/iOpenWRT/data:/app/data
    restart: unless-stopped
    env_file:
      - .env
