services:
  iopenwrt:
    build: .
    container_name: iopenwrt-controller
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    env_file:
      - .env
