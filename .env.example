# Web UI login.
# If you leave these unset, a random admin password is generated on the first run
# and printed to the logs (for Docker: `docker logs <container>`). Set your own
# before exposing this anywhere. Both names are lowercase.
# frontend_user=admin
# frontend_password=change_me

PORT=8780
